#plot_MM_direct <- function( sub, velo, velo_max, K_m ) {
# Nicht-lineare Regression
#    nls(velo ~ (velo_max * sub) / (K_m + sub),
#          start = c(velo_max, K_m)) %>%
#   fit %>%
#   print()
# Plotten des Fits
#}
